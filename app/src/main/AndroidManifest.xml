<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.tangaya.quranasrclient">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".home.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".murojaah.main.MurojaahActivity" />
        <activity android:name=".debugging.DebuggingActivity" />
        <activity android:name=".setting.ServerSettingActivity" />
        <activity android:name=".util.AudioRecordTest" />
        <activity android:name=".murojaah.chapterselection.SurahSelectionActivity" />
        <activity android:name=".murojaah.result.ResultActivity" />
        <activity android:name=".about.AboutActivity" />
        <activity android:name=".murojaah.evaldetail.EvalDetailActivity" />
        <activity android:name=".murojaah.tutorial.TutorialActivity"></activity>
    </application>

</manifest>